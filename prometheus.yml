global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets:
          - node-exporter:9100

remote_write:
  - url: http://vminsert:8480/insert/0/prometheus
